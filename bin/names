#!/usr/bin/env rc

text=`{cat}

cat <<PROMPT | chomp | turbo_instruct
Suggest nine unix-style filenames that contain the text between """.
Use underscores to separate words if there is more than one.
Text: """
$text
"""
PROMPT
